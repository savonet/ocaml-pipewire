(* open Ctypes *)

include C.Functions

(* let init a = *)
  (* let argc = allocate int (Array.length a) in  *)
  (* let argv = CArray.of_list string (Array.to_list a) in *)
  (* init argc (CArray.start argv) *)
